<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="barnett.george.budgey">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Overview_Activity">
            android:windowSoftInputMode="stateHidden"
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Info_Activity">
            android:windowSoftInputMode="stateHidden"
        </activity>

        <activity android:name=".Category_Activity">
            android:windowSoftInputMode="stateHidden"
        </activity>
        <activity android:name=".Developer_Activity">
            android:windowSoftInputMode="stateHidden"
        </activity>
        <activity android:name=".LoadingActivity">
            android:windowSoftInputMode="stateHidden"
        </activity>

        <activity android:name="barnett.george.budgey.AndroidDatabaseManager"
            android:theme="@style/Theme.AppCompat.Light"/>
    </application>

</manifest>